# Sizing browser verification

The live `/size-guide` route rendered the existing measurement guidance and the new Global fit / Choose your market conversion matrix for NG, UK, US, and EU. The route remained scannable and responsive in the browser extraction.

The live `/product/1` route rendered the Signature Linen Shirt product-specific sizing section with the Signature shirt fit chart, a Nigeria market equivalent column, centimetre measurements, and the product fit note. The product page loaded without a runtime rendering error. Screenshot capture was unavailable for both routes in the visual capture service, so browser extraction was used for route verification.
