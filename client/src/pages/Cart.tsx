import { Link } from 'wouter';
import SiteHeader from '@/components/SiteHeader';
import { ArrowRight, Minus, Plus, X } from 'lucide-react';
import { brand, formatPrice, products } from '@/lib/brand';
import { useState } from 'react';
export default function Cart(){const [qty,setQty]=useState(1);const p=products[0];return <div><SiteHeader /><main className="container py-12 md:py-20"><h1 className="font-display text-5xl">Shopping bag</h1><div className="mt-10 grid gap-12 lg:grid-cols-[1.2fr_.8fr]"><div><div className="motion-surface border-y border-[#D7C2A7] py-6"><div className="flex gap-5"><img src={p.image} alt={p.name} className="h-36 w-28 object-cover"/><div className="flex flex-1 flex-col justify-between"><div className="flex justify-between gap-3"><div><p className="text-sm">{p.name}</p><p className="mt-2 text-xs text-[#866F62]">{p.color} · {p.sizes[2]}</p></div><p className="text-sm">{formatPrice(p.price*qty)}</p></div><div className="flex items-center justify-between"><div className="flex items-center border border-[#D7C2A7]"><button onClick={()=>setQty(Math.max(1,qty-1))} className="pressable px-3 py-2"><Minus size={13}/></button><span className="w-7 text-center text-xs">{qty}</span><button onClick={()=>setQty(qty+1)} className="pressable px-3 py-2"><Plus size={13}/></button></div><div className="flex gap-5 text-[10px] uppercase tracking-[.14em] text-[#866F62]"><button>Save for later</button><button aria-label="Remove item"><X size={15}/></button></div></div></div></div></div><div className="mt-14"><p className="eyebrow text-[#866F62]">You may also like</p><div className="mt-5 grid grid-cols-3 gap-4">{products.slice(1,4).map(x=><Link key={x.id} href={`/product/${x.id}`}><img src={x.image} alt={x.name} className="aspect-[4/5] w-full object-cover"/><p className="mt-2 text-xs">{x.name}</p></Link>)}</div></div></div><aside className="motion-surface h-fit bg-[#D7C2A7] p-7 md:p-9"><h2 className="font-display text-3xl">Summary</h2><div className="mt-7 space-y-4 border-b border-[#cfc4b6] pb-6 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{formatPrice(p.price*qty)}</span></div><div className="flex justify-between"><span>Delivery</span><span>Complimentary</span></div></div><div className="flex justify-between py-6 text-base"><span>Total</span><span>{formatPrice(p.price*qty)}</span></div><Link href="/checkout" className="pressable flex items-center justify-center gap-3 bg-[#382820] py-4 text-[10px] uppercase tracking-[.16em] text-white">Proceed to checkout <ArrowRight size={15}/></Link><p className="mt-5 text-center text-xs leading-5 text-[#6f675d]">Complimentary delivery on orders over ₦150,000</p></aside></div></main></div>}
